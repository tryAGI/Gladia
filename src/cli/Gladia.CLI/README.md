# Gladia.CLI

Command-line interface for the Gladia SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Gladia.CLI --prerelease
```

## Usage

```bash
gladia --help
gladia api --help
```